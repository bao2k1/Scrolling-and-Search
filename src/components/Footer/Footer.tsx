import { StyledFooter } from "./StyledFooter"

export const Footer:React.FC<Partial<any>> = (props) => {
    return (
        <StyledFooter>
            <p>M DEVTEAM © 2024 </p>
        </StyledFooter>
    )
}

export default Footer;