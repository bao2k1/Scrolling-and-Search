import { StyledHeader } from "./StyledHeader"

const Header:React.FC<Partial<any>> = (props) => {
    return (
        <StyledHeader>
            Header DEV
        </StyledHeader>
    )
}

export default Header